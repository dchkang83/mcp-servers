# Cursor IDE와 MCP 서버 통신 규칙
- Cursor IDE와 호환되도록 MCP 프로토콜과 JSON-RPC 2.0 규약을 준수
- 모든 도구(Tool)는 명확한 입력 파라미터와 반환 타입을 가져야 함
- 리소스(Resource) URI는 표준 형식을 따라야 함 (예: resource://{param})
- 모든 응답은 JSON-RPC 2.0 형식을 준수해야 함
- 오류 응답은 표준 JSON-RPC 2.0 오류 코드를 사용해야 함
- 로깅은 표준 출력과 로거를 통해 동시에 수행해야 함
